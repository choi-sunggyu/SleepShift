<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/alarm_screen_background">

    <!-- 상단 메시지 영역 -->
    <LinearLayout
        android:id="@+id/topMessageSection"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="80dp"
        android:orientation="vertical"
        android:paddingHorizontal="32dp">

        <!-- 성규님 잘자요! -->
        <TextView
            android:id="@+id/tvGoodNightMessage"
            android:text="성규님 \n좋은 아침 입니다 !"
            android:textSize="28sp"
            android:textColor="#FFFFFF"
            android:fontFamily="sans-serif-medium"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <!-- 발바닥 코인 표시 -->
        <LinearLayout
            android:id="@+id/coinSection"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:paddingVertical="6dp">

            <ImageView
                android:layout_width="32.45dp"
                android:layout_height="32.45dp"
                android:layout_marginEnd="8dp"
                android:src="@drawable/ic_paw_coin" />

            <TextView
                android:id="@+id/tvCoinCount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif-medium"
                android:text="3"
                android:textColor="#FFFFFF"
                android:textSize="18sp" />
        </LinearLayout>
    </LinearLayout>

    <!-- 하단 잠금 해제 버튼 -->
    <LinearLayout
        android:id="@+id/bottomSection"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="60dp"
        android:orientation="vertical"
        android:gravity="center"
        android:paddingHorizontal="32dp">

        <!-- 3초 카운트다운 표시 -->
        <LinearLayout
            android:id="@+id/countdownSection"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center"
            android:visibility="gone"
            android:layout_marginBottom="16dp">

            <!-- 카운트다운 숫자 -->
            <TextView
                android:id="@+id/tvCountdown"
                android:text="3"
                android:textSize="48sp"
                android:textColor="#FFFFFF"
                android:fontFamily="sans-serif-bold"
                android:gravity="center"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

            <TextView
                android:text="길게 눌러서 알람 해제"
                android:textSize="14sp"
                android:textColor="#E2E8F0"
                android:gravity="center"
                android:layout_marginTop="8dp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
        </LinearLayout>

        <!-- 알람 해제 버튼 -->
        <androidx.cardview.widget.CardView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:cardCornerRadius="25dp"
            app:cardElevation="6dp"
            app:cardBackgroundColor="#40FFFFFF">

            <LinearLayout
                android:id="@+id/btnUnlock"
                android:layout_width="wrap_content"
                android:layout_height="70dp"
                android:orientation="horizontal"
                android:gravity="center"
                android:background="?android:attr/selectableItemBackground"
                android:paddingHorizontal="40dp">

                <!-- 자물쇠 아이콘 -->
                <ImageView
                    android:id="@+id/imgLockIcon"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:src="@drawable/ic_lock"
                    android:layout_marginEnd="12dp" />

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/tvUnlockText"
                        android:layout_width="wrap_content"
                        android:layout_height="40dp"
                        android:fontFamily="sans-serif-medium"
                        android:text="알람해제"
                        android:textColor="#000000"
                        android:textSize="30sp" />

                    <!-- 안내 메시지 -->
                    <TextView
                        android:id="@+id/tvUnlockHint"
                        android:text="해제를 원하시면 3초간 누르세요"
                        android:textSize="14sp"
                        android:textColor="#E0000000"
                        android:gravity="center"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content" />

                </LinearLayout>

            </LinearLayout>
        </androidx.cardview.widget.CardView>

    </LinearLayout>

</RelativeLayout>