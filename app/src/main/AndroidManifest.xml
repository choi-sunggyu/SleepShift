<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 권한 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SleepShift">

        <!-- AdMob App ID -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <!-- 1. 런처: 스플래시만 LAUNCHER로 설정 -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SleepShift">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. 권한 설정 화면 -->
        <activity
            android:name=".feature.PermissionActivity"
            android:exported="false"
            android:theme="@style/Theme.SleepShift" />

        <!-- 3. 온보딩 -->
        <activity
            android:name=".feature.onboarding.OnboardingActivity"
            android:exported="false" />

        <!-- 4. 설문조사 -->
        <activity
            android:name=".feature.survey.SurveyActivity"
            android:exported="false" />

        <!-- 5. 홈 -->
        <activity
            android:name=".feature.home.HomeActivity"
            android:exported="false" />

        <!-- 6. 설정 -->
        <activity
            android:name=".feature.SettingsActivity"
            android:exported="false" />

        <!-- 7. 나이트 루틴 -->
        <activity
            android:name=".feature.NightRoutineActivity"
            android:exported="false" />

        <activity
            android:name=".LockScreenActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:showWhenLocked="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="portrait" />

        <service
            android:name=".LockScreenService"
            android:enabled="true"
            android:exported="false" />

        <service
            android:name=".service.LockMonitoringService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- ⭐⭐⭐ 9. 알람 액티비티 (별도 태스크, 최우선 표시) -->
        <activity
            android:name=".feature.alarm.AlarmActivity"
            android:exported="false"
            android:launchMode="singleInstance"
            android:taskAffinity=".alarm"
            android:excludeFromRecents="true"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:showWhenLocked="true"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity" />

        <!-- 10. 리포트 -->
        <activity
            android:name=".feature.ReportActivity"
            android:exported="false" />

        <!-- 11. 모닝 루틴 -->
        <activity
            android:name=".feature.MorningRoutineActivity"
            android:exported="false" />

        <!-- 12. 로딩 -->
        <activity
            android:name=".feature.LoadingActivity"
            android:exported="false" />

        <!-- AccessibilityLockService -->
        <service
            android:name=".service.AccessibilityLockService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true"
            android:enabled="true"
            android:label="Dozeo 수면 잠금">
            <intent-filter>
                <action android:name="android.accessibility.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".service.LockAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- AlarmReceiver -->
        <receiver
            android:name=".feature.alarm.AlarmReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="com.example.sleepshift.ALARM_TRIGGER" />
                <action android:name="com.example.sleepshift.BEDTIME_NOTIFICATION" />
            </intent-filter>
        </receiver>

    </application>

</manifest>